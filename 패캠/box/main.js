const boxEl = document.querySelector('.box');

console.log(boxEl.textContent); // 여기서 출력되는 1은 숫자데이터가 아닌 문자데이터.

boxEl.textContent = 'bomvomsss!!'
console.log(boxEl.textContent);
// div 안의 내용을 바꿀 때 쓸 수 있겠군!